
// src/utils/saudacoes.ts

export function gerarSaudacao(nome: string): string {
  return `Olá, ${nome}! Seja bem-vindo(a)!`;
}

export const despedida = (nome: string): string => {
  return `Até mais, ${nome}. Volte sempre!`;
};